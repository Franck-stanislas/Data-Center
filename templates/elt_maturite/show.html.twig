{% extends 'base.html.twig' %}

{% block title 'Consulter EltMaturite' %}

{% block body %}
    <h1>{%trans%}Elément de Maturité{%endtrans%}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ elt_maturite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ elt_maturite.nom|trans }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_elt_maturite_index') }}">{%trans%}back to list{%endtrans%}</a>

    <a href="{{ path('app_elt_maturite_edit', {'id': elt_maturite.id}) }}">{%trans%}Modifier{%endtrans%}</a>

    {{ include('elt_maturite/_delete_form.html.twig') }}
{% endblock %}
