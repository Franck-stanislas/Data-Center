{% extends 'base.html.twig' %}

{% block title %}EltMaturite{% endblock %}

{% block body %}
    <h1>EltMaturite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ elt_maturite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ elt_maturite.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_elt_maturite_index') }}">back to list</a>

    <a href="{{ path('app_elt_maturite_edit', {'id': elt_maturite.id}) }}">edit</a>

    {{ include('elt_maturite/_delete_form.html.twig') }}
{% endblock %}
