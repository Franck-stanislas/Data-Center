{% extends 'base.html.twig' %}

{% block title 'Financement' %}

{% block body %}
    <h1>{%trans%}Financement{%endtrans%}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ financement.id }}</td>
            </tr>
            <tr>
                <th>Nom_financement</th>
                <td>{{ financement.nomFinancement|trans }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_financement_index') }}">{%trans%}back to list{%endtrans%}</a>

    <a href="{{ path('app_financement_edit', {'id': financement.id}) }}">{%trans%}Modifier{%endtrans%}</a>

    {{ include('financement/_delete_form.html.twig') }}
{% endblock %}
