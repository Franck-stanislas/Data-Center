{% extends 'base.html.twig' %}

{% block title %}Financement{% endblock %}

{% block body %}
    <h1>Financement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ financement.id }}</td>
            </tr>
            <tr>
                <th>Nom_financement</th>
                <td>{{ financement.nomFinancement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_financement_index') }}">back to list</a>

    <a href="{{ path('app_financement_edit', {'id': financement.id}) }}">edit</a>

    {{ include('financement/_delete_form.html.twig') }}
{% endblock %}
